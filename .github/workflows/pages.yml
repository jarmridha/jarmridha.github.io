name: Legacy portfolio deploy (manual only)
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled for automatic pushes."
